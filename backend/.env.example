# ASH Backend Configuration

# Server
BIND_ADDR=0.0.0.0
PORT=8080

# Storage Mode
# "memory" (default) - In-memory only, data lost on restart
# "sqlite" - SQLite persistence, data survives restart
# You can also specify a path like "/data/ash_relay.db"
STORAGE_MODE=memory
# STORAGE_MODE=sqlite
# SQLITE_PATH=ash_relay.db  # Default path when STORAGE_MODE=sqlite

# TTL Configuration (in seconds)
BLOB_TTL_SECS=30              # Default: 30s
BLOB_EXTENDED_TTL_SECS=172800 # Default: 48 hours
BURN_TTL_SECS=300             # Default: 5 minutes
DEVICE_TOKEN_TTL_SECS=86400   # Default: 24 hours

# Limits
MAX_CIPHERTEXT_SIZE=8192      # Default: 8KB
MAX_BLOBS_PER_CONVERSATION=50 # Default: 50
CLEANUP_INTERVAL_SECS=10      # Default: 10s

# APNS Configuration (optional)
# APNS_TEAM_ID=YOUR_TEAM_ID
# APNS_KEY_ID=YOUR_KEY_ID
# APNS_KEY_PATH=/path/to/AuthKey.p8
# APNS_BUNDLE_ID=com.yourcompany.ash
# APNS_SANDBOX=true
